syntax = "proto3";

message Account {
    string login_name = 1;
    string password = 2;
}

message Register {
    string server_ip = 1;
    uint32 server_port = 2;

    repeated Account accounts = 3;
}